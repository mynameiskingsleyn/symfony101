{% extends 'base.html.twig' %}
{% block title %}Welcome blog index {% endblock %}
{% block body %}
  how is everyone?
  {% for id, post in posts %}
    <p><a href="{{ path('blog_show',{'id':id})}}">ID {{ id }} </a> </p>
    <h3>{{ post.title }}  </h3>
    <p> {{ post.text }} </p>
  {% endfor %}
{% endblock %}
