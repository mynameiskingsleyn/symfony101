
    <div class="media text-muted pt-3">
      {% set initials = post.user.fullName|split(' ')  %}
    {#  {% set init_first = attribute(initials, 0)|first %}
      {% set init_second = attribute(initials, 1)|first  %} #}
        <img data-src="holder.js/32x32?text={{ attribute(initials, 0)|first }}{{ attribute(initials, 1)|first }}&bg=e83e8c&fg=fff&size=8" alt="" class="mr-2 rounded">
        <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
            <span class="d-block"><strong class="text-gray-dark">@{{ post.user.username }}</strong> <small>-{{ post.created_at|date('m/d/Y')}} </small></span>
            {{ post.text }}
        </p>
    </div>
